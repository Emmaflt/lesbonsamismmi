<p id="notice"><%= notice %></p>

<%
=begin%>
 
<p>
  <strong>Titre:</strong>
  <%= @offer.title %>
</p>

<p>
  <strong>Description:</strong>
  <%= @offer.description %>
</p>

<p>
  <strong>Photo:</strong>
  <%= @offer.photo %>
</p>

<p>
  <strong>Prix:</strong>
  <%= @offer.price %>
</p>

<p>
  <strong>Utilisateur Id:</strong>
  <%= @offer.user_id %>
</p>

<%= link_to 'Edit', edit_offer_path(@offer) %> |
<%= link_to 'Back', offers_path %> 
<%
=end%>


    <div class="col-md-8">
      <div class="row g-0 rounded overflow-hidden flex-md-row mb-4 h-md-250">
        <div class="col p-4 d-flex flex-column">
          <h2 class="d-inline-block mb-1"> <%= @offer.title %></h2>
          <h3 class="mb-0"><%= @offer.price %>€</h3>
          <div class="mb-1 text-muted"></div>
          <h5 class="card-text mb-auto"><%= @offer.description %></h5>
          <p class=""><%= @offer.user.username %></p> 
          <br>
           <% if current_user.present? && current_user == @offer.user %>
            <%= link_to 'Delete', @offer, method: :delete, data: { confirm: 'Are you sure?' }, class: "btn btn-danger btn-sm" %>  
           <% end %>
          <%= link_to 'Back', offers_path, class: "btn btn-primary btn-sm" %>
        </div>
        
        <div class="col-auto d-none d-lg-block">
          <svg class="bd-placeholder-img" width="auto" height="auto" xmlns="http://www.w3.org/2000/svg" role="img" aria-label="Placeholder: Thumbnail" preserveAspectRatio="xMidYMid slice" focusable="false"> <%= @offer.photo %><rect width="100%" height="100%" fill="#55595c"></rect><text x="50%" y="50%" fill="#eceeef" dy=".3em">Thumbnail</text></svg>

        </div>
      </div>
    </div>